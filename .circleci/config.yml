version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: pytest functions_testing.py

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      